version: "2"
run:
  timeout: 5m
linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic
    - gosec
    - misspell
    - revive
    - copyloopvar
    - usestdlibvars
  exclusions:
    paths:
      - web/
    rules:
      - path: _test\.go
        linters: [gosec, errcheck]
      - path: internal/pluginimpl/all/all.go
        linters: [revive]
formatters:
  enable:
    - gofmt
    - goimports
